const today=new Date().toISOString().slice(0,10);document.querySelectorAll('input[type=date]').forEach(x=>x.min=today);document.getElementById('year').textContent=new Date().getFullYear();
bookingForm.onsubmit=e=>{e.preventDefault();const d=document.getElementById('duration').value;bookingResult.innerHTML='<b>Booking details prepared!</b><br>'+name.value+' • '+phone.value+'<br>'+date.value+' at '+time.value+' • '+court.value+' • '+d+' hour(s) • ₱'+(d*150)+'<br><small>This first version does not save bookings online yet. Next we can add a free database and real availability.</small>'};
playForm.onsubmit=e=>{e.preventDefault();playResult.innerHTML='<b>Open Play registration prepared!</b><br>'+player.value+' • '+playDate.value+' at '+playTime.value+' • '+level.value+' • ₱50<br><small>Next we can add a live player list and admin confirmation.</small>'};